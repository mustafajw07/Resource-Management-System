# RmsApi

This is the RMS backend-end application built with Node.

## Prerequisites

- Node.js (18+ recommended)
- npm or yarn

## Development

Install dependencies:

```bash
npm install Or npm ci
```

Start the local development server (default port 4200):

```bash
npm start
```